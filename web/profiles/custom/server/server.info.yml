name: Server
type: profile
description: 'Custom profile to allow importing custom config'
core_version_requirement: ^8 || ^9

install:
  - node
  - block
  - dblog
  - page_cache
  - dynamic_page_cache

dependencies:
  - node
  - block

themes:
  - classy
  - seven
