name: Server
type: profile
description: 'Custom profile to allow importing custom config'
core_version_requirement: ^10 || ^11

install:
  - node
  - block
  - dblog
  - page_cache
  - dynamic_page_cache

dependencies:
  - drupal:node
  - drupal:block

themes:
  - classy
  - claro
