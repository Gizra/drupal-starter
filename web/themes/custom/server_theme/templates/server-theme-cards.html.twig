<div>
  {% if title %}
    <h2 class="text-center mb-6">
      {{ title }}
    </h2>
  {% endif %}

  <div class="flex flex-row flex-wrap space-x-4 md:space-x-8">
    {% for row in rows %}
      <div{{ row.attributes }}>
        {{- row.content -}}
      </div>
    {% endfor %}
  </div>

</div>
