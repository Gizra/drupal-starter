<div class="16 bg-auto bg-cover bg-center bg-no-repeat h-hero-header flex flex-col justify-end">
  <img class="mb-8" src="{{ background_image }}" />

  <div class="fluid-container-{{ title_container|default('narrow') }} w-full">
    <h1 class="mb-4 tracking-tight text-shadow-lg">{{ title }}</h1>
  </div>
</div>
