{% set classes = [
  'flex flex-col gap-y-12 md:gap-y-15',
  align == 'start' ? 'items-start',
  align == 'center' ? 'items-center',
  align == 'end' ? 'items-end',
] | join(' ') | trim %}

<div class="{{ classes }}">
  {{ items }}
</div>
