{#
/**
 * @file
 * Theme override to display a node card view mode.
 *
 * This serves the purpose of making sure all cards are using the full height of
 * the container. We also can't just forgo this template completely as that will
 * break contextual links for logged in users.
 */
#}
<div class="relative h-full">
  {{ title_prefix }}
  {{ title_suffix }}
  {{ content }}
</div>
