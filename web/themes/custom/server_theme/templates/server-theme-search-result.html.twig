<div class="flex flex-col gap-y-3 md:gap-y-4 border border-gray-300 rounded-lg p-5">
  {{ labels }}
  <a href="{{ url }}" class="inline-block">
    <h3 class="text-2xl md:text-3xl line-clamp-3">{{ title }}</h3>
  </a>
  <div class="line-clamp-3">{{ summary }}</div>
  <div class="text-sm text-gray-400">{{ date }}</div>
</div>
