{# Get the TW background color. #}
{% macro getBgColor(color) %}
  {% if color == 'light-gray' %}
    bg-gray-100
  {% elseif color == 'light-green' %}
    bg-green-100
  {% elseif color == 'white' %}
    bg-white
  {% else %}
    bg-transparent
  {% endif %}
{% endmacro %}

