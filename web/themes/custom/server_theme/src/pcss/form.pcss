form {

  .form-item {
    @apply flex flex-col gap-y-4;
  }

  .description {
    @apply text-gray-500 text-sm;
  }

  input.button {
    @apply bg-gray-500 hover:bg-gray-700 text-white py-2 px-4 rounded mt-4;
  }
}

