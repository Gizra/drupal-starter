<dt class="container-wide">
  <a class="title-wrapper text-gray-6" href="#{{ unique_id }}" id="{{ unique_id }}">{{ title }}</a>

  {% if link %}
    <div class="ml-4">
      <a class="text-sm" href="{{ link }}" target="_blank">{{ 'See design'|t }}</a>
    </div>
  {% endif %}
</dt>
